{
  "name": "task-stack",
  "version": "1.0.0",
  "description": "Zero-dependency task stack manager",
  "private": true,
  "scripts": {
    "build": "./tools/build.sh",
    "start": "serve dist"
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "serve": "^14.2.1"
  }
}
